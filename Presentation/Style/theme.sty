\mode<presentation>
{
  \usetheme{Berkeley}
  \useinnertheme[shadow=true]{rounded}
  \usecolortheme{whale}
  \usefonttheme{serif}  
  \setbeamertemplate{caption}[numbered]
}

% Setting Footer Properties
\makeatletter
    \setbeamertemplate{footline}%
    {%
      \leavevmode%
      \hbox{%
      \begin{beamercolorbox}[wd=1\paperwidth,ht=2.25ex,dp=1ex,right]{date in head/foot}%
        \usebeamerfont{date in head/foot}\insertframenumber{} / \inserttotalframenumber\hspace*{4ex}
      \end{beamercolorbox}}%
    }%
\makeatother

% Change standard block width
\addtobeamertemplate{block begin}{%
    \setlength{\textwidth}{0.7\textwidth}
}{}
\addtobeamertemplate{block begin}{\vskip -\smallskipamount}{}
\addtobeamertemplate{block end}{}{\vskip -\bigskipamount}

%----------------------------------------------------------------------------------------
%	PACKAGES 
%----------------------------------------------------------------------------------------
\usepackage{xcolor}         % Customize colors for hyperlinks, etc.
\usepackage{graphicx}       % Allows including images
\usepackage{booktabs}       % Allows the use of \toprule, \midrule and \bottomrule in tables

\usepackage{natbib}
\bibliographystyle{abbrvnat}

\usepackage{tikz}
\usetikzlibrary{shapes, arrows, trees, calc}

\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor={blue!50!black},
	citecolor={blue!50!black},
	urlcolor={blue!80!black}
}

\usepackage[font=tiny,skip=0pt]{caption}

\usepackage[document]{ragged2e} % Customize line allignments
\usepackage{setspace}           % Customize line spacing
\setstretch{1.25}